/**
 *
 * @param size - teste
 * @param ratio - testeee
 * @param base - testeeeeee
 * @return tedfd
 * */
const ms = (size: number, ratio: number, base: number) => (ratio ** size) * base;

export default ms;
